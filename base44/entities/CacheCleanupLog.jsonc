{
  "name": "CacheCleanupLog",
  "type": "object",
  "properties": {
    "ran_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the cleanup task ran"
    },
    "scanned": {
      "type": "number",
      "default": 0,
      "description": "Number of records scanned"
    },
    "deleted": {
      "type": "number",
      "default": 0,
      "description": "Number of records deleted"
    },
    "duration_ms": {
      "type": "number",
      "default": 0,
      "description": "Total execution time in milliseconds"
    },
    "status": {
      "type": "string",
      "enum": [
        "success",
        "partial",
        "error"
      ],
      "default": "success"
    },
    "error_message": {
      "type": "string",
      "description": "Error details if status != success"
    },
    "cleanup_reason": {
      "type": "string",
      "description": "Summary of cleanup triggers used"
    },
    "estimated_cache_bytes": {
      "type": "number",
      "default": 0,
      "description": "Estimated cache content size before smart cleanup"
    },
    "smart_deleted": {
      "type": "number",
      "default": 0,
      "description": "Records deleted by frequency-based smart cleanup"
    }
  },
  "required": [
    "ran_at",
    "status"
  ]
}